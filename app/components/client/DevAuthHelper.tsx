'use client';

/**
 * DevAuthHelper component
 * 
 * This component is DISABLED to ensure proper authentication.
 * It previously set up development mode authentication but now returns null.
 */
export default function DevAuthHelper() {
  // Disabled - no longer setting up dev mode authentication
  return null;
} 